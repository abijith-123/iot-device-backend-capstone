services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - telemetry-data:/data
    environment:
      DATABASE_URL: sqlite:////data/telemetry.db

volumes:
  telemetry-data:

